{
  "name": "backtrack_countdown",
  "train_type": "sft",
  "seed": 42,
  "output_dir": "../output_countdown/",
  "num_train": 500000,
  "dataset_percent": 2.0,
  "context_length": 128,
  "eval_steps": 5000,
  "log_steps": 10,
  "batch_size": 16,
  "gradient_accumulation_steps": 1,
  "num_train_epochs": 3,
  "weight_decay": 0.01,
  "warmup_steps": 1,
  "lr_scheduler_type": "cosine",
  "lr": 1e-5,
  "save_total_limit": 0,
  "save_steps": 10000
}
